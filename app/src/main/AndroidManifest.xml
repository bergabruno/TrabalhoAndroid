<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity
            android:name=".VisualizarFilmeActivity"
            android:exported="false"
            android:parentActivityName=".GetListaActivity" />
        />
        <activity
            android:name=".GetListaActivity"
            android:exported="false"
            android:parentActivityName=".AddFilmeActivity" />
        <activity
            android:name=".AddFilmeActivity"
            android:exported="false"
            android:parentActivityName=".FilmeActivity" />
        <activity
            android:name=".FilmeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>